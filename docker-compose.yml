services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fast_api_chat_app
    ports:
      - "8000:8000"
    env_file:
      - .secret
    restart: unless-stopped
